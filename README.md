You may edit the dict to add your own 1337code translation
```
usage: leetifier.py [-h] [-i INFILE] [-o OUTFILE] [-r REPEAT] [plaintext]

positional arguments:
  plaintext             string to leetify

options:
  -h, --help            show this help message and exit
  -i INFILE, --infile INFILE
                        batch leetify string from input file, one string on one line
  -o OUTFILE, --outfile OUTFILE
                        output file
  -r REPEAT, --repeat REPEAT
                        leetify the string X times
```
